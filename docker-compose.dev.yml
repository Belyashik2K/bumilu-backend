services:
  api:
    volumes:
      - .:/app
    command: >
      uv run uvicorn app.presentation.app:app
      --host ${RUN__HOST:-0.0.0.0}
      --port ${RUN__PORT:-8000}
      --reload
      --reload-dir /app
